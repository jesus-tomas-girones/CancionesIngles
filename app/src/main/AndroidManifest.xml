<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sacedonmg.cancionesingles">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AcercaDeActivity"
            android:label="@string/acerca_de"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".VistaCancionActivity" />
        <activity android:name=".EdicionNuevaCancionActivity" />
        <activity
            android:name=".EtiquetarCancionActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".PreferenciasActivity" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>